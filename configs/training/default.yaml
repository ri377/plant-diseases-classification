batch_size: 32
num_workers: 2
max_epochs: 5
max_lr: 0.01
weight_decay: 1.0e-4
gradient_clip_val: 0.15

accelerator: auto
devices: 1
deterministic: true
log_every_n_steps: 50

limit_train_batches: 1.0
limit_val_batches: 1.0
